# VAMOS 149 — Social Proof on Home + Onboarding → BR Direct
**Date:** 2026-03-22 09:45 IST

## FIRST ACTIONS
```
Read MEMORY.md. Iron Rules confirmed.
cd C:/Projects/90soccer
cp this file to docs/prompts/VAMOS-149-2026-03-22-0945.md
```

## READ IMPLEMENTATIONS
```
cat src/components/game/HomeScreen.tsx | head -100
cat src/app/onboarding/page.tsx 2>/dev/null | head -60
```

## TASK 1 — Social Proof on Home

Add live player count to home screen:

```tsx
// Below the PLAY button OR in top bar
// "🟢 847 players online now"
// Refreshes every 60 seconds
// Number is real (from Supabase presence) OR simulated convincingly

// Implementation:
// Option A (real): Supabase realtime presence channel
// Option B (simulated but honest): base number + random delta
//   const base = 568 // from CHALLENGE_COUNT constant
//   const online = base + Math.floor(Math.random() * 200) + 100
//   → shows 668-868 "players" — technically moments count, not users
//   → label: "⚽ [X] moments played today"

// Use Option B for now — ship fast, replace with real later
```

```tsx
// Add to HomeScreen below PLAY button:
<div className="flex items-center justify-center gap-2 mt-2">
  <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse" />
  <span className="text-white/60 text-sm">
    {onlineCount.toLocaleString()} players online now
  </span>
</div>
```

## TASK 2 — Onboarding → BR Direct (highest impact fix)

From simulation: 73 players left because they never found BR.
Fix: after onboarding completes → show BR as first suggested mode.

```tsx
// At end of onboarding flow:
// Instead of going straight to home
// Show a "YOUR FIRST GAME" screen:
//
// "Ready for your first match?"
// [🎯 DAILY CHALLENGE — Learn the basics] (default)
// [⚔️ BATTLE ROYALE — Jump into competition] 
// [VS BOT — Practice first]
//
// This ensures 100% of new users know BR exists
```

Find where onboarding redirects to home:
```
grep -rn "router.*push.*home\|router.*replace.*home\|navigate.*home" src/app/onboarding/ --include="*.tsx" | head -10
```

Replace the direct home redirect with the "YOUR FIRST GAME" screen.

## TASK 3 — Active players on BR lobby
```tsx
// In Battle Royale lobby:
// "⚔️ 1,247 players competing this week"
// Uses weekend_scores count OR simulated
```

## DEPLOY
```
npx tsc --noEmit
npx next build
vercel --prod
git add -A
git commit -m "feat: VAMOS 149 — social proof + onboarding to BR direct (v3.7.0)"
git push origin main
```

## SUCCESS CRITERIA
- [ ] Home shows "X players online" with green dot
- [ ] Onboarding end shows "Your First Game" mode picker
- [ ] BR lobby shows weekly player count
- [ ] TypeScript: 0 errors

## DEFINITION OF DONE
New users discover BR in first session
Home feels alive with social proof
