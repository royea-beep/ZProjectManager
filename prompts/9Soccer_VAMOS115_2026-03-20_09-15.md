# VAMOS 115 — 9Soccer: Save All Knowledge + Verify TestFlight
# Generated: 2026-03-20 09:15
# Project: 9Soccer | Commit: c13f9a2 | v3.3.4

You are a Senior Knowledge Engineer and iOS release specialist.
Auto-approve everything. Never ask Roye. Save everything autonomously.
Archive this prompt first, then run all tasks.

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS115_2026-03-20_09-15.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS115*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## PARALLEL — 5 agents simultaneously.

---

## TASK A — Save Master Knowledge to All Projects
Agent role: Knowledge architect who captures everything
learned so any future Claude instance can hit the ground running.

### A1. Update MEMORY.md with complete current state:
Read: C:/Users/royea/.claude/projects/C--Projects-90soccer/memory/MEMORY.md
Rewrite the top section completely with:

```markdown
## Current Production State (as of VAMOS 114 / commit c13f9a2 / v3.3.4)
- **568 challenges** (editorial max=568, next editorial=569) / ~14,200 questions
- **15 solo modes:** Classic, Timeline, Wager, Elimination, Speed Round, Bluff,
  Chain, Scout, Prediction, Versus, Memory Match, Replay, Daily Streak (ENABLED),
  Flashback (purple), Photo Round (pink, blur reveal)
- **Battle:** 3 variants + Skill Battle (36 cards) + Battle Royale (leaderboard)
- **Tournaments:** LIVE with bots + Podium + Lobby invite
- **Version:** 3.3.4 / Build auto-incremented by CI
- **Tests:** 209 passing (16 files)
- **YouTube coverage:** 100% (511/511 active challenges)

## CONFIRMED FROM ASC (screenshot 2026-03-20):
- **Bundle ID:** com.ftable.ninesoccer (NOT ninetysoccer)
- **Apple ID:** 6760544822 (NOT 6760251165 — was wrong in all files)
- **IAP IDs:** com.ftable.ninesoccer.pro_monthly / pro_annual
- **ASC URL:** appstoreconnect.apple.com/apps/6760544822

## CI Pipeline Status:
- **ios-testflight.yml:** builds + uploads IPA to ASC
- **ios-distribute.yml:** separate workflow, triggered by workflow_run,
  polls 45min for VALID build, adds to beta groups
- **Known fixes applied:** PyJWT[crypto], python3 venv, ASC API 400 fix,
  cancel-in-progress removed, Apple ID corrected, IAP IDs corrected

## Supabase Secrets (all confirmed):
ANTHROPIC_API_KEY ✅ | GITHUB_TOKEN ✅ | GOOGLE_SERVICE_ACCOUNT_JSON ✅ (FCM)
SUPABASE_ANON_KEY ✅ | SUPABASE_SERVICE_ROLE_KEY ✅ | SUPABASE_URL ✅

## GitHub Secrets (all confirmed):
ASC_API_KEY_ID ✅ | ASC_API_KEY_ISSUER_ID ✅ | ASC_KEY_P8 ✅
DISTRIBUTION_CERT_P12 ✅ | DISTRIBUTION_P12_PASSWORD ✅ | PROVISIONING_PROFILE ✅
```

### A2. Update MASTER_KNOWLEDGE.md:
```bash
cat > /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/MASTER_KNOWLEDGE.md << 'EOF'
# 9Soccer Master Knowledge
# Last updated: 2026-03-20 VAMOS 114

## IRON RULES (NEVER change):
1. VIDEO_DURATION = 9s
2. Solo/Battle SEPARATE
3. Static export only
4. C14 = draft permanently
5. content_engine = ON
6. Challenge IDs permanent
7. App name = "9Soccer", tagline = "9 seconds. Pure football."
8. NO App Store steps unless Roye says "מכינים לאפ סטור"

## BUNDLE ID TRUTH (confirmed from ASC):
- App Bundle: com.ftable.ninesoccer
- IAP Monthly: com.ftable.ninesoccer.pro_monthly
- IAP Annual: com.ftable.ninesoccer.pro_annual
- Apple ID: 6760544822
- Supabase: psxqlmgsifvsmiijkucu

## DB ID GAP:
Content engine: 427-488
Editorial: 489-568 (C427-C506)
Next: 569
ALWAYS: SELECT MAX(id) FROM solo_challenges before insert

## CI PIPELINE LESSONS:
1. Split build/distribute into 2 separate workflow files
2. Never cancel-in-progress on distribute workflow
3. Use PyJWT[crypto] not bare PyJWT
4. Use python3 -m venv before pip (macOS 15)
5. ASC API: no filter[processingState]=VALID (deprecated 2025)
6. Always verify Bundle ID matches ASC

## KEY PATHS:
- Project: C:/Projects/90soccer/
- Dashboard: C:/Projects/9soccer-dashboard/
- Memory: C:/Users/royea/.claude/projects/C--Projects-90soccer/memory/
- Prompts: C:/Projects/90soccer/docs/prompts/
           C:/Projects/ZProjectManager/prompts/
- Screenshots: C:/Projects/90soccer/screenshots/asc-ready/
EOF
```

### A3. Save to ZProjectManager too:
```bash
cp /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/MASTER_KNOWLEDGE.md \
   /c/Projects/ZProjectManager/9SOCCER_MASTER_KNOWLEDGE.md
echo "Saved to ZProjectManager"
```

### A4. Create/update GEMS_AND_UTILS.md:
```bash
cat > /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/GEMS_AND_UTILS.md << 'EOF'
# 9Soccer Gems, Utils & Reusable Patterns
# Last updated: 2026-03-20

## iOS CI/CD GEM — Split Workflow Pattern:
Problem: distribute step cancelled by new pushes
Solution: ios-distribute.yml as separate workflow_run-triggered file
File: .github/workflows/ios-distribute.yml
Reusable for: any iOS app with long ASC distribution wait

## SUPABASE GEM — FCM Fallback:
GOOGLE_SERVICE_ACCOUNT_JSON → FCM credential
getFcmCredentials() in _shared/fcm-auth.ts
Auto-extracts project_id from JSON
Reusable for: any app needing Firebase push notifications

## BUG REPORTER GEM — Video Cascade:
iOS doesn't support getDisplayMedia
Cascade: screen → camera → audio
Runtime MIME detection: webm/mp4
Edge Function: Claude AI analysis → GitHub Issue
Files: src/app/bug-report/, supabase/functions/analyze-bug-report/

## ASC API GEM — Query Tool:
scripts/query-asc.js — queries bundle ID + IAP from ASC
scripts/asc-status.sh — shell version
Lesson: filter[processingState]=VALID deprecated in 2025

## CONTENT ENGINE GEM:
DB-backed with TS fallback
Deterministic rotation using epoch/modulo
Exact-date override capability
files: src/lib/content-repository.ts

## DNA QUIZ GEM — Player Photo Quiz:
3 rounds × 3 player photos
calculateDnaFromPicks() → majority vote
QuizResult → character reveal animation
Files: src/components/onboarding/PlayerQuiz.tsx
       src/components/onboarding/QuizResult.tsx
       src/lib/player-quiz.ts

## SEASON PASS GEM:
XP → DB sync via sync_season_xp RPC
localStorage + Supabase dual storage
WC2026 countdown: new Date('2026-06-11')
DNA family bonus: +25% XP when challenge matches

## ECONOMY GEM:
earnCoins() → auto-triggers season XP
economy:updated events for UI sync
Pro multiplier: 2x via applyProMultiplier()
File: src/lib/economy.ts
EOF
```

### A5. Save workflow guide:
```bash
cat > /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/WORKFLOW_GUIDE.md << 'EOF'
# VAMOS Workflow Guide
# Last updated: 2026-03-20

## THE TRIANGLE:
Roye (Hebrew, strategic) ↔ Claude (strategic brain, English prompts) ↔ Claude Bot (executor)

## VAMOS CYCLE:
1. Roye: "כן" or describes problem
2. Claude: analyzes, creates .md prompt file
3. File saved to /mnt/user-data/outputs/
4. Roye: downloads → drags to Claude Bot
5. Bot: archives prompt, runs 4-5 parallel agents
6. Bot: pastes output → Roye pastes here
7. Claude: summarizes, updates memory, prepares next

## PROMPT FILENAME FORMAT:
{Project}_VAMOS{N}_{YYYY-MM-DD}_{HH-MM}.md
Example: 9Soccer_VAMOS115_2026-03-20_09-15.md

## ARCHIVE LOCATIONS (always both):
C:/Projects/90soccer/docs/prompts/
C:/Projects/ZProjectManager/prompts/

## PROMPT STRUCTURE:
- Role: specific expert with depth ("Senior iOS Release Engineer who has shipped 50+ apps")
- Auto-approve: "Never ask Roye. Fix autonomously."
- Archive first: always before other tasks
- 4-5 parallel agents
- Task E = pipeline (tsc + test + build + git + MEMORY)
- Structured final report

## COMMUNICATION RULES:
- Claude → Roye: Hebrew, concise, tables for results
- Claude → Bot: English, in .md files only
- Bot auto-approves everything
- Claude asks max 1 question per turn
- Always think ahead, suggest next step
- Prompts as downloadable files, never in chat

## SPRINT STRUCTURE (5 agents):
A: Feature/Fix 1
B: Feature/Fix 2
C: Feature/Fix 3
D: Feature/Fix 4
E: Pipeline (waits for A-D, then tsc+test+build+git+memory)
EOF
```

---

## TASK B — Save Reusable Skills for Other Projects
Agent role: Senior developer extracting reusable patterns.

### B1. Create iOS CI skill in ZProjectManager:
```bash
mkdir -p /c/Projects/ZProjectManager/skills/ios-ci/
cat > /c/Projects/ZProjectManager/skills/ios-ci/README.md << 'EOF'
# iOS CI/CD Skill — TestFlight Distribution
## Learned from: 9Soccer VAMOS 97-114

## THE CRITICAL PATTERN:
Split build + distribute into 2 separate workflow files.
Never use cancel-in-progress on the distribute workflow.

## Files:
- ios-testflight.yml: builds IPA, uploads to ASC
- ios-distribute.yml: triggered by workflow_run, adds to TestFlight groups

## Required GitHub Secrets:
- ASC_API_KEY_ID, ASC_API_KEY_ISSUER_ID, ASC_KEY_P8
- DISTRIBUTION_CERT_P12, DISTRIBUTION_P12_PASSWORD
- PROVISIONING_PROFILE (base64 encoded)

## Key Fixes:
1. python3 -m venv before pip (macOS 15 PEP 668)
2. PyJWT[crypto] not bare PyJWT (ES256 needs cryptography)
3. ASC API: no filter[processingState]=VALID (deprecated 2025)
4. Build number: auto from github.run_number
5. Always verify Bundle ID before first upload

## Template files:
See C:/Projects/90soccer/.github/workflows/
EOF

cp /c/Projects/90soccer/.github/workflows/ios-testflight.yml \
   /c/Projects/ZProjectManager/skills/ios-ci/ios-testflight.template.yml
cp /c/Projects/90soccer/.github/workflows/ios-distribute.yml \
   /c/Projects/ZProjectManager/skills/ios-ci/ios-distribute.template.yml
echo "iOS CI skill saved"
```

### B2. Create BugReporter skill:
```bash
mkdir -p /c/Projects/ZProjectManager/skills/bug-reporter/
cat > /c/Projects/ZProjectManager/skills/bug-reporter/README.md << 'EOF'
# BugReporter Skill
## Learned from: 9Soccer VAMOS 97

## What it does:
Tester sees bug → taps 🐛 → records video →
uploads → Claude AI analyzes → GitHub Issue created →
appears in Dashboard → assigned to sprint → fixed

## iOS Video Cascade:
1. getDisplayMedia (screen+audio) — desktop/web
2. getUserMedia with video — iOS camera fallback
3. getUserMedia audio only — last resort
Runtime MIME: webm on desktop, mp4 on iOS

## Required Supabase:
- Table: bug_reports
- Storage bucket: bug-report-videos
- Edge Function: analyze-bug-report
- Secrets: ANTHROPIC_API_KEY, GITHUB_TOKEN

## Template files:
See C:/Projects/90soccer/src/app/bug-report/
    C:/Projects/90soccer/supabase/functions/analyze-bug-report/
EOF
echo "BugReporter skill saved"
```

### B3. Create VAMOS methodology skill:
```bash
mkdir -p /c/Projects/ZProjectManager/skills/vamos-methodology/
cp /c/Projects/ZProjectManager/docs/VAMOS-METHODOLOGY-GUIDE.md \
   /c/Projects/ZProjectManager/skills/vamos-methodology/
cat >> /c/Projects/ZProjectManager/skills/vamos-methodology/VAMOS-METHODOLOGY-GUIDE.md << 'EOF'

## UPDATES FROM 9Soccer VAMOS 85-114:

### New: Prompt as downloadable file
Always deliver mega-prompts as .md files, not in chat.
Filename: {Project}_VAMOS{N}_{YYYY-MM-DD}_{HH-MM}.md

### New: Archive locations (always both):
C:/Projects/{project}/docs/prompts/
C:/Projects/ZProjectManager/prompts/

### New: Bot auto-approval
"Auto-approve everything. Never ask Roye. Fix autonomously."
Bot answers its own confirmation prompts with YES.

### New: Specific expert roles in prompts
"You are a Senior iOS Release Engineer who has shipped 50+ apps"
Depth of role description improves output quality significantly.

### New: Always think ahead rule
Claude always suggests next VAMOS even before current finishes.
EOF
echo "VAMOS methodology skill updated"
```

### B4. Create ASC API skill:
```bash
mkdir -p /c/Projects/ZProjectManager/skills/asc-api/
cp /c/Projects/90soccer/scripts/query-asc.js \
   /c/Projects/ZProjectManager/skills/asc-api/query-asc.template.js
cat > /c/Projects/ZProjectManager/skills/asc-api/README.md << 'EOF'
# ASC API Skill
## Learned from: 9Soccer VAMOS 112-113

## Key lessons:
1. filter[processingState]=VALID → 400 error (deprecated 2025)
   Fix: remove filter, check attribute in response
2. Always query real Bundle ID from ASC before assuming
3. Apple ID ≠ always what you think — verify from ASC screenshot

## JWT generation (Python):
```python
import jwt, time
payload = {
    'iss': issuer_id,
    'iat': int(time.time()),
    'exp': int(time.time()) + 1200,
    'aud': 'appstoreconnect-v1'
}
token = jwt.encode(payload, private_key,
    algorithm='ES256',
    headers={'kid': key_id})
```

## Required:
- ASC_API_KEY_ID (from App Store Connect)
- ASC_API_KEY_ISSUER_ID
- ASC_KEY_P8 (private key content, NOT file path)
EOF
echo "ASC API skill saved"
```

### B5. Commit all skills:
```bash
cd /c/Projects/ZProjectManager
git add skills/ 2>/dev/null || true
git add prompts/ 2>/dev/null || true
git add 9SOCCER_MASTER_KNOWLEDGE.md 2>/dev/null || true
git commit -m "feat: 9Soccer skills — iOS CI, BugReporter, VAMOS methodology, ASC API" \
  2>/dev/null || echo "ZProjectManager commit done or no git"
```

---

## TASK C — Verify TestFlight Build
Agent role: TestFlight distribution specialist.

### C1. Check if build arrived:
```bash
gh run list --workflow=ios-testflight.yml \
  --repo royea-beep/9soccer --limit=3 \
  --json status,conclusion,headSha,databaseId \
  --jq '.[] | "\(.status) | \(.conclusion) | \(.headSha[0:7])"'
```

### C2. Check distribute workflow:
```bash
gh run list --workflow=ios-distribute.yml \
  --repo royea-beep/9soccer --limit=3 \
  --json status,conclusion,headSha,databaseId \
  --jq '.[] | "\(.status) | \(.conclusion) | \(.headSha[0:7])"'
```

### C3. If latest distribute succeeded:
Try to get TestFlight public link via ASC API:
```bash
cd /c/Projects/90soccer
node scripts/query-asc.js 2>&1 | head -20
```

### C4. Check what build number is in ASC:
```bash
gh run list --workflow=ios-testflight.yml \
  --repo royea-beep/9soccer --limit=1 \
  --json databaseId --jq '.[0].databaseId' | \
xargs -I{} gh run view {} \
  --repo royea-beep/9soccer --log 2>&1 | \
grep -iE "build.*number|CFBundleVersion|Bump build|build_number" | \
head -5
```

### C5. Report TestFlight status clearly.

---

## TASK D — Create TestFlight Public Link Guide
Agent role: TestFlight distribution manager.

### D1. Update docs/testflight/PUBLIC_LINK.md
with correct Apple ID:
```bash
cat > /c/Projects/90soccer/docs/testflight/PUBLIC_LINK.md << 'EOF'
# How to Get TestFlight Public Link
# App: 9Soccer | Apple ID: 6760544822

## Steps (2 minutes):
1. Go to: https://appstoreconnect.apple.com/apps/6760544822/testflight
2. Click on the latest build
3. Click "Groups" in left sidebar
4. Click "External Testers" group
5. Toggle "Enable Public Link" to ON
6. Copy the link (format: testflight.apple.com/join/XXXXXXXX)

## Share with invite template:
See: docs/testflight/INVITE_TEMPLATE.md

## Current build:
v3.3.4 | Bundle: com.ftable.ninesoccer | Apple ID: 6760544822
EOF
```

### D2. Update INVITE_TEMPLATE.md with correct URL:
```bash
sed -i 's|apps/6760251165|apps/6760544822|g' \
  /c/Projects/90soccer/docs/testflight/INVITE_TEMPLATE.md
sed -i 's|apps/6760251165|apps/6760544822|g' \
  /c/Projects/90soccer/docs/testflight/TESTER_REGISTRY.md
```

### D3. Commit docs:
```bash
cd /c/Projects/90soccer
git add docs/
git commit -m "docs: VAMOS 115 — correct Apple ID in testflight docs"
git push origin main
```

---

## TASK E — Final Pipeline + Memory Update
Agent role: Release coordinator.

### E1. Run checks:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -5
npm test 2>&1 | tail -3
```

### E2. Update MEMORY.md next sprint:
Change "Next sprint: VAMOS 115" to "Next sprint: VAMOS 116"

### E3. Final commit:
```bash
git add -A
git commit -m "docs: VAMOS 115 — master knowledge save, skills archive, testflight docs corrected"
git push origin main
npx cap sync ios
```

### E4. Final report:
```
VAMOS 115 COMPLETE
==================

📚 Knowledge saved to:
- C:/Users/royea/.claude/projects/.../memory/ (all files updated)
- C:/Projects/ZProjectManager/skills/ (4 new skills)
- C:/Projects/ZProjectManager/prompts/ (all 115 prompts)
- C:/Projects/90soccer/docs/ (testflight docs corrected)

📱 TestFlight status: [build status]

🔗 TestFlight link: [if available]
   OR: Get from ASC → appstoreconnect.apple.com/apps/6760544822/testflight

📨 Ready to invite testers:
   docs/testflight/INVITE_TEMPLATE.md

→ Next: VAMOS 116 = first tester bug fixes
```

---
END VAMOS 115
