# VAMOS 109 — 9Soccer Debug Sprint + TestFlight Push
# Generated: 2026-03-20 08:05
# Project: 9Soccer | Commit: 0b02046 | v3.3.4

You are a Senior iOS Debug Engineer and QA Lead.
Your mission: fix all known issues, push a clean build to TestFlight,
and prepare the app for real testers.

RULES:
- Auto-approve ALL your own prompts — never ask Roye
- Try everything autonomously first
- Archive this prompt before anything else

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS109_2026-03-20_08-05.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS109*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## CONTEXT — Known issues to fix:

From docs/testflight/KNOWN_ISSUES.md +
docs/DEBUG_SPRINT_PREP.md +
VAMOS 98-108 audit findings:

1. Push notifications: FCM not confirmed working
2. RevenueCat API key not in Vercel env
3. CI distribution timeout (fixed to 40min in V108)
4. DNA quiz photos may fail on slow connections
5. Season Pass XP sync — fixed in V98, needs E2E verify
6. BugReporter 🐛 button — confirm visible in TestFlight build

---

## PARALLEL SPRINT — 5 agents simultaneously.

---

## TASK A — Fix RevenueCat + Push Notifications
Agent role: Senior iOS monetization and notifications engineer.

### A1. Fix RevenueCat API key in Vercel:
```bash
# Check current Vercel env
cat /c/Projects/90soccer/vercel.json
grep -r "REVENUECAT\|RC_API\|PURCHASES" \
  /c/Projects/90soccer/src/ --include="*.ts" \
  --include="*.tsx" | head -10
```

The correct key is: appl_WsnUMUMWcxEmMWYiyBumbucCiqe

Check if it's already in .env.local:
```bash
grep "REVENUECAT\|RC_API" /c/Projects/90soccer/.env.local 2>/dev/null
```

If not set, add to .env.example:
```
NEXT_PUBLIC_RC_API_KEY=appl_WsnUMUMWcxEmMWYiyBumbucCiqe
```

Also add to vercel.json env section so
it's baked into the static export.

### A2. Verify push notification secrets:
```bash
npx supabase secrets list \
  --project-ref psxqlmgsifvsmiijkucu 2>/dev/null
```

Check these are set:
- ANTHROPIC_API_KEY ✅ (set in V98)
- GITHUB_TOKEN ✅ (set in V98)
- GOOGLE_SERVICE_ACCOUNT_JSON (FCM fallback)

If GOOGLE_SERVICE_ACCOUNT_JSON exists,
push notifications may already work.
Test by triggering push-test function:
```bash
npx supabase functions invoke push-test \
  --project-ref psxqlmgsifvsmiijkucu \
  --body '{"test": true}' 2>/dev/null | tail -10
```

### A3. Test the full push notification flow:
```bash
# Invoke daily challenge push in dry-run mode
npx supabase functions invoke push-daily-challenge \
  --project-ref psxqlmgsifvsmiijkucu \
  --body '{}' 2>/dev/null | tail -10
```

Report: is it dry_run: true or actually sending?

### A4. Fix any issues found.
TypeScript: zero errors.

---

## TASK B — Full E2E Game Flow Verification
Agent role: QA engineer doing pre-release regression testing.

### B1. Read and verify the critical paths:
- src/app/page.tsx — onboarding flow order
- src/lib/content-repository.ts — daily challenge fetch
- src/lib/economy.ts — coin/XP flow
- src/lib/season-pass.ts — XP sync to DB

### B2. Verify onboarding order is correct:
The flow must be EXACTLY:
1. OnboardingFlow (if not done) — Welcome + HowItWorks + PlayerQuiz + QuizResult
2. OrientationPicker (if not done)
3. DailySkillQuiz (if not done today)
4. Game

Check page.tsx implements this order correctly.
Fix if wrong.

### B3. Verify coin flow E2E:
```bash
# Check sync_season_xp RPC exists in DB
npx supabase db query --linked \
  "SELECT routine_name FROM information_schema.routines
   WHERE routine_name = 'sync_season_xp';" 2>/dev/null
```

```bash
# Check season_xp column exists
npx supabase db query --linked \
  "SELECT column_name FROM information_schema.columns
   WHERE table_name = 'profiles'
   AND column_name IN ('season_xp', 'season_level',
   'daily_streak_count', 'notif_daily');" 2>/dev/null
```

### B4. Verify daily challenge rotation:
```bash
npx supabase db query --linked \
  "SELECT id, slug, title, status
   FROM solo_challenges
   WHERE status = 'active'
   ORDER BY id DESC LIMIT 5;" 2>/dev/null
```

Check content_engine flag is ON:
```bash
npx supabase db query --linked \
  "SELECT key, enabled FROM feature_flags
   WHERE key IN ('content_engine', 'bug_reporter',
   'daily_streak_mode');" 2>/dev/null
```

### B5. Report all verification results.
Fix any issues found.

---

## TASK C — Visual: Final Polish Pass
Agent role: UI/UX engineer doing pre-launch polish.

### C1. Read docs/VISUAL_QA_CHECKLIST.md
Check every item systematically.

### C2. Fix the most impactful visual issues:

Priority fixes:
1. Ensure HomeScreen shows coin balance
   prominently (top right)
2. Ensure the PLAY button is the biggest
   element on the screen
3. Ensure Season Pass strip shows correct
   next milestone
4. Ensure DNA character is shown on profile

### C3. Check and fix loading states:
- Does HomeScreen show skeleton while loading?
- Does the game show a spinner before video loads?
- Does the leaderboard show skeleton?

Add any missing loading states.

### C4. Check error states:
- If daily challenge fails to load, what happens?
- If user has no internet, what shows?

Add friendly error messages if missing:
"No connection — check your internet and try again 🔄"

### C5. TypeScript: zero errors.

---

## TASK D — TestFlight: Force Distribution
Agent role: CI/CD engineer.

### D1. Check current CI status:
```bash
gh run list --workflow=ios-testflight.yml \
  --limit=3 \
  --json status,conclusion,headSha,createdAt \
  --jq '.[] | "\(.status) \(.conclusion) \(.headSha[0:7]) \(.createdAt)"'
```

### D2. Check if build 0b02046 was distributed:
```bash
LATEST=$(gh run list --workflow=ios-testflight.yml \
  --limit=1 --json databaseId --jq '.[0].databaseId')
gh run view "$LATEST" --log 2>&1 | \
  grep -E "distribut|tester|group|success|error|timeout" | \
  tail -20
```

### D3. Force a new CI run with empty commit
to trigger fresh build + 40min distribution:
```bash
cd /c/Projects/90soccer
git commit --allow-empty \
  -m "ci: force TestFlight distribution (40min poll)"
git push origin main
sleep 5
gh run list --workflow=ios-testflight.yml --limit=1 \
  --json url --jq '.[0].url'
```

### D4. Update docs/testflight/BUILD_HISTORY.md
with current build status.

### D5. Create TestFlight public link instructions:
Add to docs/testflight/TESTER_REGISTRY.md:

```markdown
## How to get the public TestFlight link:
1. appstoreconnect.apple.com/apps/6760251165/testflight
2. Click on your build
3. Click "Groups" → "External Testers"
4. Enable "Public Link"
5. Copy and share the link

## Sharing message template:
"🎮 You're invited to test 9Soccer!
The football trivia game where every question
is 9 seconds.
Install TestFlight, then join:
[YOUR LINK HERE]"
```

---

## TASK E — Pipeline: v3.3.5 + Final Commit
Agent role: Release engineer.

### E1. Run all checks:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -10
npm test 2>&1 | tail -5
npm run build 2>&1 | tail -5
```

### E2. Fix any errors.

### E3. Bump to v3.3.4 → stays 3.3.4
(no version bump needed — this is bug fixes
within the same version)

### E4. Commit all changes:
```bash
git add -A
git commit -m "fix: VAMOS 109 — RevenueCat env, push notif verified, E2E checks, visual polish, force TestFlight distribution"
git push origin main
npx cap sync ios
```

### E5. Update MEMORY.md:
- All E2E verifications passed/fixed
- RevenueCat key in env
- Push notification status confirmed
- Visual polish complete
- TestFlight: new CI run triggered
- Next sprint: VAMOS 110 — first real
  tester bug fixes

### E6. Final report:
```
VAMOS 109 COMPLETE
==================

✅ Fixed:
- RevenueCat API key: [in Vercel / confirmed]
- Push notifications: [sending / dry-run]
- E2E flow: [verified / fixed X issues]
- Visual polish: [X fixes]
- TestFlight: new build triggered

📱 TestFlight CI run: [URL]
   Testers will receive update in ~40 min

🎯 App is ready for real testers.

⚠️ Still manual:
1. ASC → add build to TestFlight group
   (OR wait for CI run above)
2. Share TestFlight public link with testers

→ Next: VAMOS 110 = analyze tester feedback
  + fix bugs from BugReporter
```

---
END VAMOS 109
