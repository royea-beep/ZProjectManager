# VAMOS 130 — Fix Top Issues from Expert Simulation
**Date:** 2026-03-21 19:30 IST

## FIRST ACTIONS
```
Read MEMORY.md. Iron Rules confirmed.
cd C:/Projects/90soccer
cp this file to docs/prompts/VAMOS-130-2026-03-21-1930.md
```

## FIX 1 — Remove floating football balls + fix empty space on Home
```
grep -rn "floating\|float.*ball\|ball.*float\|animate.*ball\|⚽.*absolute\|absolute.*⚽" src/components/game/HomeScreen.tsx | head -10
```
Remove all decorative floating ball elements.
Fix the empty space: The Lemon mascot should sit closer to Today's Challenge card.
Reduce top padding/margin between Season Pass and the challenge card.

## FIX 2 — Rematch button after Battle Royale loss
```
grep -rn "game.*over\|GameOver\|battle.*result\|BattleResult\|rematch\|play.*again" src/app/battle-royale/ --include="*.tsx" -l 2>/dev/null
cat src/app/battle-royale/page.tsx | grep -A20 "lose\|lost\|defeat\|game.*over" | head -30
```

Add rematch button:
```tsx
// After game over screen
<button
  onClick={() => router.push('/battle-royale?rematch=1')}
  className="bg-orange-500 hover:bg-orange-600 text-white font-bold py-3 px-8 rounded-full text-lg"
>
  🔄 Rematch
</button>
```

## FIX 3 — Haptic feedback on iPhone
```
grep -rn "haptic\|vibrate\|navigator.vibrate" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | head -10
```

Add haptic utility:
```typescript
// src/lib/haptics.ts
export function haptic(style: 'light' | 'medium' | 'heavy' | 'success' | 'error' = 'light') {
  if (typeof navigator !== 'undefined' && navigator.vibrate) {
    const patterns = { light: [10], medium: [20], heavy: [30], success: [10, 50, 10], error: [30, 20, 30] }
    navigator.vibrate(patterns[style])
  }
}
```

Add haptic calls to:
- Answer selection (light)
- Correct answer (success)
- Wrong answer (error)  
- PLAY button press (medium)
- Battle Royale elimination (heavy)

## FIX 4 — Profile per-mode stats breakdown
```
cat src/app/profile/page.tsx | head -80
grep -rn "stats\|Stats\|played\|wins\|score" src/app/profile/ --include="*.tsx" | head -20
```

Add to Profile Stats tab:
```tsx
// Per-mode breakdown table
const modes = ['Daily Challenge', 'Battle Royale', 'Showdown', 'Tournament', 'Wager']
modes.map(mode => (
  <div key={mode} className="flex justify-between items-center py-2 border-b border-white/10">
    <span>{mode}</span>
    <span>{stats[mode]?.played ?? 0} played | {stats[mode]?.avgScore ?? 0} avg</span>
  </div>
))
```

## FIX 5 — Squad invite WhatsApp deep-link
```
grep -rn "invite\|Invite\|share.*squad\|squad.*share" src/app/squad/ --include="*.tsx" | head -10
```

Change invite link to WhatsApp deep-link:
```typescript
const inviteViaWhatsApp = (squadCode: string) => {
  const text = encodeURIComponent(`הצטרף לסקווד שלי ב-9Soccer! קוד: ${squadCode}\nhttps://9soccer.vercel.app/squad/join/${squadCode}`)
  window.open(`https://wa.me/?text=${text}`, '_blank')
}
```

## QUALITY CHECK
```
npx tsc --noEmit
npx vitest run
npx next build
vercel --prod
git add -A
git commit -m "fix: VAMOS 130 — floating balls, rematch, haptics, profile stats, squad WhatsApp (v3.5.4)"
git push origin main
```

## SUCCESS CRITERIA
- [ ] No floating balls on home
- [ ] Empty space reduced
- [ ] Rematch button after Battle loss
- [ ] Haptic feedback on answer tap
- [ ] Profile shows per-mode stats
- [ ] Squad invite opens WhatsApp

## DEFINITION OF DONE
Top 5 expert simulation issues fixed — deployed v3.5.4
