# VAMOS — Force Remove Skills + vs Bot + Empty Space
**Date:** 2026-03-22 08:20 IST

## PROBLEM
Screenshot still shows:
1. Skills section (ACTIVE / The Lemon Guard / Ice Queen / The Professor) — bottom right
2. vs Bot button — bottom left (purple)
3. Large empty space between Season Pass and Challenge card
4. Season 2 banner still visible

## TASK

### Step 1 — Find EXACTLY where Skills renders on home
```
cd C:/Projects/90soccer
grep -rn "SKILLS\|SkillSlot\|skill.*slot\|ACTIVE.*skill\|The Lemon Guard\|Ice Queen\|The Professor" src/components/game/HomeScreen.tsx
grep -n "skill\|Skill\|SKILL" src/components/game/HomeScreen.tsx
```

Whatever lines are found → DELETE THEM COMPLETELY.

### Step 2 — Find EXACTLY where vs Bot renders on page.tsx
```
grep -n "vs.*Bot\|vsBot\|VsBot\|vs-bot\|purple" src/app/page.tsx
grep -n "vs.*Bot\|vsBot" src/components/game/Game.tsx
```

DELETE those lines completely.

### Step 3 — Find the empty space
```
# Search for what's between Season Pass and Challenge card
grep -n "empty\|spacer\|Season.*2\|season.*banner\|Road.*to.*WC\|days.*until" src/components/game/HomeScreen.tsx | head -20
```

The "Season 2: Road to WC 2026" + "days until the World Cup 84" section is still rendering.
DELETE it — this content belongs in the Season Pass expanded view, not on home.

### Step 4 — Verify Season Pass is compact
```
grep -n "compact\|SeasonPass\|season.*pass" src/components/game/HomeScreen.tsx | head -10
cat src/components/shared/SeasonPassStrip.tsx | head -30
```

Make sure `compact={true}` is passed and the strip shows ONLY:
- Level indicator
- Progress bar
- One line max

### Step 5 — Final check: count floating elements
```
grep -n "fixed\|z-[0-9]" src/components/game/HomeScreen.tsx src/app/page.tsx | grep -v "//\|import" | head -20
```

Expected: only 2 floating elements
- BATTLE MODE button
- Bug reporter FAB

### Step 6 — Deploy
```
npx tsc --noEmit
npx next build
vercel --prod
git add -A
git commit -m "fix: force remove skills+vsBot+season2 from home (v3.6.0)"
git push origin main
```

## SUCCESS CRITERIA
- [ ] Skills = 0 occurrences on home
- [ ] vs Bot = 0 occurrences on home/page.tsx
- [ ] Season 2 banner = gone
- [ ] Only 2 floating buttons: BATTLE MODE + bug reporter
- [ ] Home shows: top bar → season pass (compact) → challenge card → PLAY → lemon → mode cards

## DEFINITION OF DONE
Clean home screenshot — no clutter
