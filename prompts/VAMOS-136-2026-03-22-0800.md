# VAMOS — Fix Home Overcrowding
**Date:** 2026-03-22 08:00 IST

## FIRST ACTIONS
```
Read MEMORY.md. Iron Rules confirmed.
cd C:/Projects/90soccer
```

## WHAT WAS SEEN IN SCREENSHOT
1. Skills section STILL showing on home (should be in Profile only)
2. Large empty gap between Season Pass and Today's Challenge
3. Season Pass strip too tall
4. 3 floating buttons at bottom (vs Bot + BATTLE MODE + bug reporter)

## FIXES

### Fix 1 — Remove Skills from HomeScreen completely
```
grep -n "Skill\|skill\|SKILL\|ACTIVE\|SkillSlot" src/components/game/HomeScreen.tsx | head -20
```
Remove the entire Skills section from HomeScreen.tsx.
It should ONLY exist in Profile → Stats tab.

### Fix 2 — Remove empty gap
```
grep -n "empty\|spacer\|h-\|min-h-\|py-\|mt-\|mb-" src/components/game/HomeScreen.tsx | head -30
```
Find what's causing the large empty space between Season Pass and Today's Challenge.
Reduce or remove unnecessary margins/padding.

### Fix 3 — Make Season Pass strip compact
Reduce height of Season Pass:
- Remove the XP milestone icons row (too much detail for home)
- Keep only: level + progress bar + "Next reward" text
- Max height: 60px

### Fix 4 — Reduce bottom buttons
Currently 3 floating buttons: vs Bot (purple) + BATTLE MODE (orange) + bug reporter (red)
- Keep: BATTLE MODE (orange) — most important CTA
- Keep: bug reporter (red, small)  
- REMOVE: vs Bot from home bottom bar — it should be accessible from Modes only
  OR make vs Bot smaller and secondary

```
grep -n "vs.*Bot\|vsBot\|vs-bot" src/components/game/HomeScreen.tsx src/app/page.tsx | head -10
```

### Fix 5 — Fix The Lemon position
The Lemon appears too far left, floating awkwardly.
Move it to be closer to the PLAY button — it should feel like it's "introducing" the challenge.

## DEPLOY
```
npx tsc --noEmit
npx next build
vercel --prod
git add -A
git commit -m "fix: home screen declutter — remove skills, fix gap, compact season pass (v3.5.9)"
git push origin main
```

## SUCCESS CRITERIA
- [ ] Skills NOT visible on home
- [ ] No large empty gaps
- [ ] Season Pass max 60px tall
- [ ] Max 2 floating buttons at bottom
- [ ] The Lemon near PLAY button
- [ ] Home feels like Brawl Stars — clean, one focus point

## DEFINITION OF DONE
Screenshot of home looks clean and focused
