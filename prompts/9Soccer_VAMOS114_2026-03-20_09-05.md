# VAMOS 114 — 9Soccer: Fix Apple ID + IAP Product IDs
# Generated: 2026-03-20 09:05
# Project: 9Soccer | Commit: 8fa43e7 | v3.3.4

You are a Senior iOS Release Engineer.
Auto-approve everything. Never ask Roye. Fix autonomously.
Archive this prompt first, then run all tasks.

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS114_2026-03-20_09-05.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS114*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## CONFIRMED FROM ASC (screenshot verified):
- Bundle ID: com.ftable.ninesoccer ✅
- SKU: ninesoccer
- Apple ID (real): 6760544822
- Apple ID (wrong, used in code): 6760251165

TWO fixes needed:
1. Replace wrong Apple ID everywhere
2. Revert IAP product IDs to ninesoccer (V105 was wrong)

---

## PARALLEL — 4 agents simultaneously.

---

## TASK A — Fix Apple ID Everywhere
Agent role: iOS release engineer fixing App ID references.

### A1. Find all occurrences of wrong Apple ID:
```bash
grep -rn "6760251165" \
  /c/Projects/90soccer/ \
  --include="*.ts" --include="*.tsx" \
  --include="*.yml" --include="*.yaml" \
  --include="*.json" --include="*.md" \
  --include="*.sh" --include="*.rb" \
  --exclude-dir=node_modules \
  --exclude-dir=.next \
  --exclude-dir=.git \
  2>/dev/null | grep -v ".next/" | head -30
```

### A2. Replace ALL occurrences:
```bash
cd /c/Projects/90soccer

# Replace in all relevant files
find . -type f \( \
  -name "*.ts" -o -name "*.tsx" \
  -o -name "*.yml" -o -name "*.yaml" \
  -o -name "*.json" -o -name "*.md" \
  -o -name "*.sh" -o -name "*.rb" \
\) \
  -not -path "*/node_modules/*" \
  -not -path "*/.next/*" \
  -not -path "*/.git/*" \
  -exec grep -l "6760251165" {} \; | \
while read f; do
  sed -i 's/6760251165/6760544822/g' "$f"
  echo "Fixed: $f"
done
```

### A3. Verify replacement:
```bash
grep -rn "6760251165" \
  /c/Projects/90soccer/ \
  --exclude-dir=node_modules \
  --exclude-dir=.next \
  --exclude-dir=.git \
  2>/dev/null | grep -v "Binary" | head -10
echo "Remaining occurrences of old ID above (should be 0)"
```

### A4. Verify new ID is correct everywhere:
```bash
grep -rn "6760544822" \
  /c/Projects/90soccer/ \
  --exclude-dir=node_modules \
  --exclude-dir=.next \
  --exclude-dir=.git \
  2>/dev/null | head -20
```

### A5. TypeScript: zero errors after fix.

---

## TASK B — Fix IAP Product IDs
Agent role: iOS IAP specialist reverting incorrect V105 fix.

### B1. Find all IAP product ID references:
```bash
grep -rn "ninetysoccer\|ninesoccer" \
  /c/Projects/90soccer/src/ \
  --include="*.ts" --include="*.tsx" \
  2>/dev/null | head -20
```

### B2. Revert ninetysoccer → ninesoccer
in IAP-related files:
```bash
cd /c/Projects/90soccer

# Fix iap.ts
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/lib/iap.ts

# Fix pro page
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/app/pro/page.tsx

# Fix ProPaywall
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/components/ProPaywall.tsx

# Fix dashboard data
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/app/dashboard/data.ts

# Fix dashboard page
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/app/dashboard/page.tsx

# Fix fcm-token.ts comment
sed -i 's/com\.ftable\.ninetysoccer/com.ftable.ninesoccer/g' \
  src/lib/fcm-token.ts
```

### B3. Verify all IAP IDs are now correct:
```bash
grep -rn "ftable\." \
  /c/Projects/90soccer/src/ \
  --include="*.ts" --include="*.tsx" \
  2>/dev/null | grep -v "//\|Binary" | head -20
```

Should see ONLY `com.ftable.ninesoccer.*`

### B4. Also fix docs that reference wrong IAP IDs:
```bash
grep -rn "ninetysoccer" \
  /c/Projects/90soccer/docs/ \
  /c/Projects/90soccer/fastlane/ \
  2>/dev/null | head -10

# Fix them
find /c/Projects/90soccer/docs \
     /c/Projects/90soccer/fastlane \
  -type f -exec grep -l "ninetysoccer" {} \; | \
while read f; do
  sed -i 's/ninetysoccer/ninesoccer/g' "$f"
  echo "Fixed doc: $f"
done
```

### B5. TypeScript: zero errors.

---

## TASK C — Fix CI Workflow Apple ID References
Agent role: CI/CD engineer.

### C1. Fix Apple ID in CI workflows:
```bash
grep -rn "6760251165\|ninetysoccer" \
  /c/Projects/90soccer/.github/ \
  2>/dev/null | head -20
```

Fix all occurrences:
```bash
find /c/Projects/90soccer/.github \
  -type f \
  -exec grep -l "6760251165\|ninetysoccer" {} \; | \
while read f; do
  sed -i 's/6760251165/6760544822/g' "$f"
  sed -i 's/ninetysoccer/ninesoccer/g' "$f"
  echo "Fixed CI file: $f"
done
```

### C2. Verify CI workflow is correct:
```bash
grep -n "6760\|ninesoccer\|ninetysoccer" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -20
grep -n "6760\|ninesoccer\|ninetysoccer" \
  /c/Projects/90soccer/.github/workflows/ios-distribute.yml | head -10
```

### C3. Run TypeScript check:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -10
```

### C4. Run tests:
```bash
npm test 2>&1 | tail -5
```

### C5. Report all files changed.

---

## TASK D — Commit + Trigger Clean Build
Agent role: Release engineer.

### D1. Stage all changes:
```bash
cd /c/Projects/90soccer
git add -A
git status --short
```

### D2. Commit:
```bash
git commit -m "fix: VAMOS 114 — correct Apple ID (6760544822) + IAP IDs (ninesoccer)

- Apple ID was wrong everywhere: 6760251165 → 6760544822
- IAP product IDs reverted to match real bundle ID:
  com.ftable.ninesoccer.pro_monthly/annual
- V105 IAP 'fix' was incorrect (changed to ninetysoccer wrongly)
- All docs, CI workflows, src files updated

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push origin main
npx cap sync ios
```

### D3. Update MEMORY.md:
- Apple ID corrected: 6760544822 (was 6760251165)
- Bundle ID confirmed: com.ftable.ninesoccer
- IAP IDs corrected: com.ftable.ninesoccer.*
- Next sprint: VAMOS 115

### D4. Report new CI run URL.

### D5. Final report:
```
VAMOS 114 COMPLETE
==================

✅ Apple ID fixed: 6760251165 → 6760544822
✅ IAP IDs fixed: ninetysoccer → ninesoccer
✅ All files updated: src/, docs/, .github/, fastlane/

Files changed: [list]
Commit: [hash]

📱 New CI run: [URL]
   Build 378 should appear in TestFlight
   in ~45 minutes

→ Next: VAMOS 115 = verify clean build
  + get TestFlight public link
  + send invites to testers
```

---
END VAMOS 114
