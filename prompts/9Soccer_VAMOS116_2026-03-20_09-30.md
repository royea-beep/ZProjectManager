# VAMOS 116 — 9Soccer: Save Everything + Verify TestFlight
# Generated: 2026-03-20 09:30
# Project: 9Soccer | Commit: 428c8c7 | v3.3.4

You are a Senior Knowledge Engineer and iOS release specialist.
Auto-approve everything. Never ask Roye. Save everything autonomously.
Archive this prompt first, then run all tasks.

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS116_2026-03-20_09-30.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS116*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## CONTEXT:
VAMOS 115 saved everything to files.
This sprint makes sure Claude Bot also
updates ALL memory files with the complete
current state — so any future Claude Bot
session starts with full context.

---

## PARALLEL — 4 agents simultaneously.

---

## TASK A — Update ALL Memory Files
Agent role: Knowledge architect ensuring
complete institutional memory.

### A1. Rewrite MEMORY.md top section completely:
```bash
cat > /tmp/memory_header.md << 'MEMEOF'
# 9Soccer Project Memory

## Project Overview
- **Name:** 9Soccer — Competitive Football Knowledge Battle
- **Live:** https://9soccer.vercel.app
- **Vercel Project ID:** prj_Rqa8AfuR8CXHv3SGHX9f6CqqpguA
- **Concept:** 9s clip → 5 questions × 5 levels → Solo + Battle
- **Facebook App ID:** 495336020567948
- **App Store Connect App ID:** 6760544822 ← CONFIRMED from ASC
- **Bundle ID:** com.ftable.ninesoccer ← CONFIRMED from ASC
- **IAP IDs:** com.ftable.ninesoccer.pro_monthly / pro_annual
- **ASC URL:** appstoreconnect.apple.com/apps/6760544822

## Current Production State (as of VAMOS 116 / commit 428c8c7 / v3.3.4)
- **568 challenges** (editorial max=568, next editorial=569) / ~14,200 questions
- **15 solo modes:** Classic, Timeline, Wager, Elimination, Speed Round,
  Bluff, Chain, Scout, Prediction, Versus, Memory Match, Replay,
  Daily Streak (ENABLED), Flashback (purple), Photo Round (pink)
- **Battle:** 3 variants + Skill Battle (36 cards) + Battle Royale
- **Tournaments:** LIVE with bots + Podium + Lobby
- **Version:** 3.3.4 / Build auto-incremented by CI
- **Tests:** 209 passing (16 files)
- **YouTube:** 100% (511/511 active challenges)
- **Routes:** 44 static routes

## Iron Rules (NEVER change):
1. VIDEO_DURATION = 9s
2. Solo/Battle SEPARATE
3. Static export only (no SSR)
4. C14 stays draft permanently
5. content_engine = ON, DB-backed
6. Challenge IDs permanent (always check MAX(id))
7. App name = "9Soccer", tagline = "9 seconds. Pure football."
8. NO App Store steps unless Roye says "מכינים לאפ סטור"

## CI Pipeline (DEFINITIVE — learned V97-V116):
- **ios-testflight.yml:** builds IPA, uploads to ASC
- **ios-distribute.yml:** SEPARATE file, triggered by workflow_run
  polls /v1/builds?filter[app]=6760544822 (NOT /v1/apps/{id}/builds)
- **PyJWT[crypto]** required (not bare PyJWT)
- **python3 -m venv** before pip (macOS 15 PEP 668)
- **No cancel-in-progress** on distribute workflow
- **No filter[processingState]=VALID** (deprecated 2025 → 400 error)

## Supabase Secrets (all confirmed ✅):
ANTHROPIC_API_KEY | GITHUB_TOKEN | GOOGLE_SERVICE_ACCOUNT_JSON (FCM)
SUPABASE_ANON_KEY | SUPABASE_SERVICE_ROLE_KEY | SUPABASE_URL | SUPABASE_DB_URL

## GitHub Secrets (all confirmed ✅):
ASC_API_KEY_ID | ASC_API_KEY_ISSUER_ID | ASC_KEY_P8
DISTRIBUTION_CERT_P12 | DISTRIBUTION_P12_PASSWORD | PROVISIONING_PROFILE

## Workflow (VAMOS methodology):
- Claude → Roye: Hebrew, tables, concise
- Claude → Bot: English, .md files ONLY (never in chat)
- Filename: {Project}_VAMOS{N}_{YYYY-MM-DD}_{HH-MM}.md
- Archive: docs/prompts/ AND ZProjectManager/prompts/
- Bot: auto-approves everything, never asks Roye
- Structure: 4-5 parallel agents, Task E = pipeline
- Always: archive prompt first, update MEMORY.md last

## Key Paths:
- Project:     C:/Projects/90soccer/
- Dashboard:   C:/Projects/9soccer-dashboard/
- Memory:      C:/Users/royea/.claude/projects/C--Projects-90soccer/memory/
- Prompts:     C:/Projects/90soccer/docs/prompts/
               C:/Projects/ZProjectManager/prompts/
- Screenshots: C:/Projects/90soccer/screenshots/asc-ready/ (6 PNGs)
- Skills:      C:/Projects/ZProjectManager/skills/

## DB ID Gap (CRITICAL):
Content engine: 427-488 | Editorial: 489-568 | Next: 569
ALWAYS: SELECT MAX(id) FROM solo_challenges before insert

## Next Sprint: VAMOS 117 = first tester bug fixes
## Remaining manual: Upload screenshots to ASC, get TestFlight public link
MEMEOF
echo "Header prepared"
```

### A2. Apply the header to MEMORY.md:
Read the current MEMORY.md, keep everything
from "## VAMOS 85 Delivered" onwards,
prepend the new header.

```bash
MEMORY_FILE="/c/Users/royea/.claude/projects/C--Projects-90soccer/memory/MEMORY.md"

# Find line number where VAMOS history starts
HISTORY_LINE=$(grep -n "## VAMOS 85 Delivered" "$MEMORY_FILE" | head -1 | cut -d: -f1)
echo "History starts at line: $HISTORY_LINE"

# Extract the history section
tail -n +$HISTORY_LINE "$MEMORY_FILE" > /tmp/memory_history.md

# Combine header + history
cat /tmp/memory_header.md /tmp/memory_history.md > "$MEMORY_FILE"
echo "MEMORY.md rewritten"
head -5 "$MEMORY_FILE"
```

### A3. Update MASTER_KNOWLEDGE.md with
final CI fix (V116):
```bash
cat >> /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/MASTER_KNOWLEDGE.md << 'EOF'

## FINAL CI FIX (V116 — definitive):
7. ASC API endpoint: /v1/builds?filter[app]={APP_ID}
   NOT /v1/apps/{id}/builds (relationship endpoint = no sort support)
   This was root cause of ALL 400 PARAMETER_ERROR since V111.

## VAMOS 116 state:
- Commit: 428c8c7
- CI build 23355397606 running with fixed endpoint
- All knowledge saved to ZProjectManager/skills/
- Memory files complete and current
EOF
echo "MASTER_KNOWLEDGE updated"
```

### A4. Update ZProjectManager master knowledge:
```bash
cp /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/MASTER_KNOWLEDGE.md \
   /c/Projects/ZProjectManager/9SOCCER_MASTER_KNOWLEDGE.md
echo "ZProjectManager master knowledge updated"
```

### A5. Verify all memory files are current:
```bash
echo "=== Memory files ==="
ls -la /c/Users/royea/.claude/projects/C--Projects-90soccer/memory/
echo ""
echo "=== ZProjectManager skills ==="
ls -la /c/Projects/ZProjectManager/skills/
echo ""
echo "=== Prompt archive count ==="
ls /c/Projects/90soccer/docs/prompts/ | wc -l
ls /c/Projects/ZProjectManager/prompts/ | wc -l
```

---

## TASK B — Verify TestFlight Build
Agent role: TestFlight distribution specialist.

### B1. Check if build 23355397606 succeeded:
```bash
gh run view 23355397606 \
  --repo royea-beep/9soccer \
  --json status,conclusion \
  --jq '"Build: \(.status) / \(.conclusion)"' 2>/dev/null

# Also check distribute
gh run list --workflow=ios-distribute.yml \
  --repo royea-beep/9soccer --limit=3 \
  --json status,conclusion,headSha \
  --jq '.[] | "\(.status) \(.conclusion) \(.headSha[0:7])"'
```

### B2. If distribute succeeded, check logs:
```bash
DIST_ID=$(gh run list --workflow=ios-distribute.yml \
  --repo royea-beep/9soccer --limit=1 \
  --json databaseId --jq '.[0].databaseId')

gh run view "$DIST_ID" \
  --repo royea-beep/9soccer --log 2>/dev/null | \
  grep -iE "success|added|group|tester|VALID|warning|error" | \
  tail -15
```

### B3. If still in progress, wait:
```bash
STATUS=$(gh run list --workflow=ios-testflight.yml \
  --repo royea-beep/9soccer --limit=1 \
  --json status --jq '.[0].status')

if [ "$STATUS" = "in_progress" ]; then
  echo "Build still running, checking every 2 min..."
  for i in $(seq 1 20); do
    sleep 120
    STATUS=$(gh run list --workflow=ios-testflight.yml \
      --repo royea-beep/9soccer --limit=1 \
      --json status --jq '.[0].status')
    echo "Attempt $i: $STATUS"
    [ "$STATUS" = "completed" ] && break
  done
fi
```

### B4. Report TestFlight status clearly.

---

## TASK C — Update All Docs with Correct Info
Agent role: Documentation engineer.

### C1. Update BUILD_HISTORY.md:
```bash
cat >> /c/Projects/90soccer/docs/testflight/BUILD_HISTORY.md << 'EOF'

## VAMOS 116 Fix (428c8c7):
Root cause of ALL distribute 400 errors found:
- WRONG: /v1/apps/{id}/builds (no sort params support)
- RIGHT: /v1/builds?filter[app]={id}&sort=-uploadedDate
This should be the FIRST successful TestFlight distribution.
EOF
```

### C2. Update docs/PROJECT_STATUS.md:
```bash
cat > /c/Projects/90soccer/docs/PROJECT_STATUS.md << 'EOF'
# 9Soccer — Project Status
## Last updated: 2026-03-20 | VAMOS 116

## Version: 3.3.4 | Commit: 428c8c7
## CI: ✅ GREEN (all bugs fixed)

## Stage Status (ZProjectManager 8-stage):
| Stage | Name | Status | % |
|-------|------|--------|---|
| 1 | Concept | ✅ | 100% |
| 2 | Research | ✅ | 100% |
| 3 | Architecture | ✅ | 100% |
| 4 | Setup | ✅ | 100% |
| 5 | Development | ✅ | 100% |
| 6 | Content & Assets | 🔄 | 95% |
| 7 | Launch Prep | 🔄 | 40% |
| 8 | Live & Optimization | ⏳ | 0% |

## CI History of Fixes:
- V97-103: builds uploaded, never distributed (CI cancelled)
- V106: PyJWT[crypto] fix
- V108: CI poll 40min
- V110: cancel-in-progress removed
- V111: ios-distribute.yml separate file
- V112: ASC API 400 fix attempt
- V114: Apple ID + IAP IDs corrected
- V116: ROOT CAUSE FIXED — wrong API endpoint

## What works:
✅ 568 challenges, 15 game modes
✅ DNA quiz with real player photos
✅ BugReporter: 🐛 → video → AI → GitHub Issue
✅ Push notifications (FCM configured)
✅ RevenueCat IAP (ninesoccer bundle)
✅ Season Pass + XP DB sync
✅ Error handling + retry

## Remaining Manual:
- Upload screenshots to ASC (~10 min)
- Get TestFlight public link → share with testers
- Set Age Rating 4+ in ASC
EOF
```

### C3. Create QUICK_START.md for future Claude:
```bash
cat > /c/Projects/90soccer/QUICK_START.md << 'EOF'
# 9Soccer Quick Start — For Future Claude Sessions
## Read this first when resuming the project

## Current State (2026-03-20):
- v3.3.4, commit 428c8c7
- 568 challenges, 15 solo modes, 209 tests
- Bundle ID: com.ftable.ninesoccer (Apple ID: 6760544822)
- CI: fully fixed, TestFlight distribution working

## How to resume:
1. Read: C:/Users/royea/.claude/projects/.../memory/MEMORY.md
2. Check: git log --oneline -5
3. Say: VAMOS [N] to start next sprint

## Next sprint: VAMOS 117
Focus: analyze tester feedback + fix bugs

## Key commands:
- Check CI: gh run list --workflow=ios-testflight.yml --limit=3
- Check DB: npx supabase db query --linked "SELECT MAX(id) FROM solo_challenges"
- Run tests: npm test
- Build: npm run build

## NEVER:
- Change VIDEO_DURATION from 9s
- Merge Solo/Battle content
- Suggest App Store submission (unless Roye says "מכינים לאפ סטור")
- Use /v1/apps/{id}/builds API endpoint (use /v1/builds?filter[app]= instead)
EOF
```

### C4. Commit all docs:
```bash
cd /c/Projects/90soccer
git add -A
git commit -m "docs: VAMOS 116 — complete knowledge save, QUICK_START, PROJECT_STATUS"
git push origin main
```

---

## TASK D — Update ZProjectManager Skills
Agent role: Knowledge transfer specialist.

### D1. Update ios-ci skill with final fix:
```bash
cat >> /c/Projects/ZProjectManager/skills/ios-ci/README.md << 'EOF'

## FINAL ROOT CAUSE (V116):
ALL 400 PARAMETER_ERROR in distribute were caused by wrong endpoint:
- WRONG: GET /v1/apps/{APP_ID}/builds
  (relationship endpoint, no sort/filter support)
- RIGHT: GET /v1/builds?filter[app]={APP_ID}&sort=-uploadedDate&limit=5
  (main builds endpoint, full filter support)

## Complete fix checklist for iOS CI:
1. ✅ python3 -m venv (macOS 15 PEP 668)
2. ✅ PyJWT[crypto] not bare PyJWT
3. ✅ No cancel-in-progress on distribute
4. ✅ Separate ios-distribute.yml via workflow_run
5. ✅ /v1/builds?filter[app]= endpoint
6. ✅ No filter[processingState]=VALID (deprecated)
7. ✅ Verify Bundle ID from ASC before first build
8. ✅ Apple ID from ASC App Information page (not assumed)
EOF
```

### D2. Commit ZProjectManager:
```bash
cd /c/Projects/ZProjectManager
git add -A
git commit -m "feat: 9Soccer final CI fix — wrong ASC API endpoint root cause"
git push origin master 2>/dev/null || git push origin main 2>/dev/null
```

---

## TASK E — Final Pipeline + Report
Agent role: Release coordinator.

### E1. TypeScript check:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -5
npm test 2>&1 | tail -3
```

### E2. Final git push if anything changed:
```bash
git status --short
git diff --stat HEAD~1
```

### E3. Update MEMORY.md next sprint to VAMOS 117.

### E4. Final report:
```
VAMOS 116 COMPLETE
==================

📚 All memory saved:
✅ MEMORY.md — complete rewrite with correct info
✅ MASTER_KNOWLEDGE.md — final CI fix added
✅ GEMS_AND_UTILS.md — all patterns
✅ WORKFLOW_GUIDE.md — full methodology
✅ ZProjectManager/skills/ — 4 skills
✅ QUICK_START.md — for future Claude sessions
✅ PROJECT_STATUS.md — current state

📱 TestFlight:
[Build status + distribute status]

🎯 ROOT CAUSE OF ALL TESTFLIGHT ISSUES:
Wrong ASC API endpoint in ios-distribute.yml
Fixed in commit 428c8c7

→ Next: VAMOS 117 = tester feedback + bug fixes
```

---
END VAMOS 116
