# VAMOS 112 — 9Soccer Fix Invalid Binary + TestFlight
# Generated: 2026-03-20 08:35
# Project: 9Soccer | Commit: 06a7963 | v3.3.4

You are a Senior iOS Release Engineer.
Auto-approve everything. Never ask Roye. Fix autonomously.
Archive this prompt first, then run all tasks.

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS112_2026-03-20_08-35.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS112*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## CONTEXT:
TestFlight shows Build 18 = "Invalid Binary".
Builds from VAMOS 97-111 (v3.3.x) are not appearing.
The CI uploads succeed but Apple rejects the binary.

---

## PARALLEL — 4 agents simultaneously.

---

## TASK A — Diagnose Invalid Binary
Agent role: iOS build engineer who has fixed every
type of App Store binary rejection.

### A1. Check latest CI logs for upload errors:
```bash
LATEST=$(gh run list --workflow=ios-testflight.yml \
  --limit=1 --json databaseId --jq '.[0].databaseId')
echo "Latest run: $LATEST"

gh run view "$LATEST" --log 2>&1 | \
  grep -iE "error|invalid|reject|upload|success|altool|transporter|ipa|xcrun" | \
  head -40
```

### A2. Check the distribute workflow too:
```bash
gh run list --workflow=ios-distribute.yml \
  --limit=3 \
  --json status,conclusion,headSha,databaseId \
  --jq '.[] | "\(.status) \(.conclusion) \(.headSha[0:7])"' 2>/dev/null || \
echo "ios-distribute.yml not found or no runs yet"

DIST_ID=$(gh run list --workflow=ios-distribute.yml \
  --limit=1 --json databaseId --jq '.[0].databaseId' 2>/dev/null)
[ -n "$DIST_ID" ] && gh run view "$DIST_ID" --log 2>&1 | \
  grep -iE "error|invalid|reject|upload|success|group|tester" | head -30
```

### A3. Check what version/build number is in Info.plist:
```bash
grep -A1 "CFBundleVersion\|CFBundleShortVersionString" \
  /c/Projects/90soccer/ios/App/App/Info.plist | head -6
```

### A4. Common Invalid Binary causes — check each:

**A. Build number not incrementing:**
```bash
# Check if CI auto-increments build number
grep -n "BUILD_NUMBER\|CFBundleVersion\|increment\|build_number" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -20
```

**B. Wrong bundle ID:**
```bash
grep -n "BUNDLE_ID\|bundle_id\|com\.ftable" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -10
```

**C. Signing issues:**
```bash
grep -n "CERT\|PROFILE\|signing\|CODE_SIGN\|PROVISIONING" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -20
```

**D. Wrong export method:**
```bash
grep -n "export_method\|exportMethod\|app-store\|ad-hoc" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -10
```

### A5. Read the full workflow to understand
exactly what it does for signing and upload:
```bash
cat /c/Projects/90soccer/.github/workflows/ios-testflight.yml
```

Report the exact cause of Invalid Binary.

---

## TASK B — Fix the Invalid Binary Issue
Agent role: iOS code signing and distribution expert.

### B1. Based on Task A findings, apply the fix.

Most likely fixes:

**If build number is not incrementing:**
Add to workflow before xcodebuild:
```bash
# Auto-increment build number
BUILD_NUM=${{ github.run_number }}
/usr/libexec/PlistBuddy -c \
  "Set :CFBundleVersion $BUILD_NUM" \
  ios/App/App/Info.plist
```

**If wrong export method:**
Change to `app-store` (not `ad-hoc`):
```yaml
xcodebuild -exportArchive \
  -archivePath build/9Soccer.xcarchive \
  -exportPath build/export \
  -exportOptionsPlist ExportOptions.plist
```

Check/create ExportOptions.plist:
```bash
cat /c/Projects/90soccer/ios/App/ExportOptions.plist 2>/dev/null || \
cat > /c/Projects/90soccer/ios/App/ExportOptions.plist << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>method</key>
  <string>app-store</string>
  <key>teamID</key>
  <string>3K9KJN9L9U</string>
  <key>uploadBitcode</key>
  <false/>
  <key>uploadSymbols</key>
  <true/>
  <key>signingStyle</key>
  <string>automatic</string>
</dict>
</plist>
EOF
```

**If Capacitor version mismatch:**
```bash
cd /c/Projects/90soccer
npx cap sync ios 2>&1 | tail -5
```

### B2. Apply all fixes found.

### B3. Commit fixes:
```bash
git add -A
git commit -m "fix: VAMOS 112 — fix Invalid Binary in TestFlight ([reason])"
git push origin main
```

### B4. Trigger new CI build:
```bash
sleep 5
gh run list --workflow=ios-testflight.yml \
  --limit=1 --json url --jq '.[0].url'
```

---

## TASK C — Verify Capacitor + iOS Config
Agent role: Capacitor iOS specialist.

### C1. Check Capacitor config is correct:
```bash
cat /c/Projects/90soccer/capacitor.config.ts
cat /c/Projects/90soccer/ios/App/App/capacitor.config.json
```

### C2. Verify bundle ID matches everywhere:
```bash
grep -r "com\.ftable\.ninetysoccer\|bundleId\|BUNDLE_ID" \
  /c/Projects/90soccer/capacitor.config.ts \
  /c/Projects/90soccer/ios/App/App/Info.plist \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -15
```

### C3. Sync Capacitor:
```bash
cd /c/Projects/90soccer
npx cap sync ios 2>&1 | tail -5
```

### C4. Check if there are any Xcode project
settings that could cause rejection:
```bash
grep -r "DEVELOPMENT_TEAM\|CODE_SIGN\|PROVISIONING_PROFILE" \
  /c/Projects/90soccer/ios/App/App.xcodeproj/ 2>/dev/null | \
  grep -v ".pbxproj:.*\/\/" | head -10
```

### C5. Report Capacitor/iOS config status.

---

## TASK D — Update Docs + MEMORY
Agent role: Documentation engineer.

### D1. Update docs/testflight/BUILD_HISTORY.md:
Add entry for current situation.

### D2. Update docs/PROJECT_STATUS.md:
Note the Invalid Binary issue and fix applied.

### D3. Commit docs:
```bash
cd /c/Projects/90soccer
git add docs/
git commit -m "docs: VAMOS 112 — build history + invalid binary status"
git push origin main
npx cap sync ios
```

### D4. Update MEMORY.md with findings and fix.

### D5. Final report:
```
VAMOS 112 COMPLETE
==================

🔍 Invalid Binary cause: [exact cause]

✅ Fix applied: [what was changed]

📱 New CI run: [URL]
   Expected in TestFlight: ~45 minutes

→ Next: VAMOS 113 = verify build in TestFlight
  + send invites to testers
```

---
END VAMOS 112
