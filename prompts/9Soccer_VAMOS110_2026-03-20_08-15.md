# VAMOS 110 — 9Soccer CI Status + TestFlight Verify + Next Steps
# Generated: 2026-03-20 08:15
# Project: 9Soccer | Commit: b1ae3a1 | v3.3.4

You are a Senior iOS Release Engineer and CI/CD specialist.
Auto-approve everything. Never ask Roye. Find solutions autonomously.
Archive this prompt first, then run all tasks.

---

## FIRST ACTION — Archive:
```bash
PROMPT_NAME="9Soccer_VAMOS110_2026-03-20_08-15.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true
for f in /c/Users/royea/Downloads/9Soccer_VAMOS110*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## PARALLEL — 4 agents simultaneously.

---

## TASK A — CI Status: Verify b1ae3a1 Build
Agent role: CI/CD engineer who owns the release pipeline.

### A1. Check the latest CI runs:
```bash
gh run list --workflow=ios-testflight.yml \
  --limit=5 \
  --json status,conclusion,headSha,databaseId,url,createdAt \
  --jq '.[] | "\(.status) | \(.conclusion) | \(.headSha[0:7]) | \(.createdAt)"'
```

### A2. Find the run for commit b1ae3a1:
```bash
RUN_ID=$(gh run list --workflow=ios-testflight.yml \
  --limit=10 \
  --json databaseId,headSha \
  --jq '.[] | select(.headSha | startswith("b1ae3a1")) | .databaseId')

if [ -z "$RUN_ID" ]; then
  # Try latest run
  RUN_ID=$(gh run list --workflow=ios-testflight.yml \
    --limit=1 --json databaseId --jq '.[0].databaseId')
fi

echo "Run ID: $RUN_ID"
gh run view "$RUN_ID" 2>/dev/null | head -20
```

### A3. If run is still in_progress, watch it:
```bash
gh run watch "$RUN_ID" --exit-status 2>&1 | tail -20
```

### A4. If run failed, diagnose and fix:
```bash
gh run view "$RUN_ID" --log-failed 2>&1 | head -60
```

Common fixes:
- PyJWT error → already fixed (venv)
- ES256 error → already fixed (PyJWT[crypto])
- Timeout → poll is now 40min
- Node.js deprecation warning → update to v4 actions

Fix any issues found. Commit and push fix.
Trigger new run:
```bash
gh workflow run ios-testflight.yml --ref main
```

### A5. Report final CI status clearly.

---

## TASK B — Screenshots: Use ExplainIt Project
Agent role: Screenshot automation engineer.

### B1. Check ExplainIt project:
```bash
ls /c/Projects/ExplainIt/ 2>/dev/null | head -20
cat /c/Projects/ExplainIt/README.md 2>/dev/null | head -30
cat /c/Projects/ExplainIt/package.json 2>/dev/null | grep -E "script|description" | head -10
```

### B2. Understand what ExplainIt does:
Read its main functionality.
Can it take screenshots of web pages?
What tools does it use (Playwright? Puppeteer?)?

### B3. If ExplainIt can take screenshots:
Use it to take screenshots of 9Soccer web app.
The web app is live at:
- https://9soccer.vercel.app (check if live first)
- OR run locally: cd /c/Projects/90soccer && npm run build && npx serve out -p 3000

Take screenshots of these 6 screens:
1. / (HomeScreen)
2. /modes (ModesHub)
3. /leaderboard
4. /profile
5. /pro/upgrade
6. /daily-streak

Save to: /c/Projects/90soccer/screenshots/asc-ready/

### B4. If ExplainIt cannot do this directly:
Check if it has Playwright or Puppeteer:
```bash
cat /c/Projects/ExplainIt/package.json | grep -E "playwright|puppeteer|screenshot"
```

If yes, write a quick script using
ExplainIt's existing setup.

### B5. Report screenshot status:
- Got screenshots? ✅ + dimensions
- Not possible with ExplainIt? Explain why
  + suggest next approach

---

## TASK C — Node.js Actions Deprecation Fix
Agent role: GitHub Actions maintenance engineer.

### C1. The CI warning says:
"Node.js 20 actions are deprecated.
actions/checkout@v4, actions/setup-node@v4
will be forced to run with Node.js 24"

Fix this proactively in the workflow:
```bash
grep -n "actions/checkout\|actions/setup-node\|actions/upload-artifact\|actions/cache" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml | head -20
```

### C2. Update all actions to latest versions:
- actions/checkout@v4 → stays v4 (already latest)
- actions/setup-node@v4 → stays v4 (already latest)
- Check if any actions still on v3

```bash
grep -n "@v3\|@v2\|@v1" \
  /c/Projects/90soccer/.github/workflows/ios-testflight.yml
```

Replace any @v3 with @v4.

### C3. Also check screenshots workflow:
```bash
grep -n "@v3\|@v2\|@v1" \
  /c/Projects/90soccer/.github/workflows/screenshots.yml 2>/dev/null
```

Fix any old versions found.

### C4. Commit fixes:
```bash
git add .github/workflows/
git commit -m "ci: update deprecated Node.js 20 actions to latest versions"
git push origin main
```

### C5. Report what was updated.

---

## TASK D — TestFlight Tester Registry Update
Agent role: TestFlight distribution manager.

### D1. Read current tester registry:
```bash
cat /c/Projects/90soccer/docs/testflight/TESTER_REGISTRY.md
```

### D2. Update build history:
```bash
cat > /c/Projects/90soccer/docs/testflight/BUILD_HISTORY.md << 'EOF'
# 9Soccer — TestFlight Build History

| Build | Version | Commit | Date | Status | Notes |
|-------|---------|--------|------|--------|-------|
| auto | 3.3.4 | b1ae3a1 | 2026-03-20 | 🔄 CI running | VAMOS 109 — RevenueCat, error handling |
| auto | 3.3.4 | dd90503 | 2026-03-20 | ✅ Uploaded | VAMOS 106 — PyJWT[crypto] fix |
| auto | 3.3.3 | 42209b9 | 2026-03-20 | ⚠️ Partial | TestFlight welcome, first testers |
| 338 | 3.2.0 | old | 2026-03-18 | 📦 Old | Pre-CI-fix builds |

## Latest Build Notes (v3.3.4):
- ✅ OnboardingFlow + PlayerQuiz wired
- ✅ DNA quiz: 3 player photos, 3 rounds
- ✅ BugReporter: 🐛 button + video recording
- ✅ 568 challenges, 15 game modes
- ✅ RevenueCat IAP live
- ✅ Push notifications ready (FCM configured)
- ✅ Error handling: retry on network failure
EOF
```

### D3. Add tester invitation template:
```bash
cat > /c/Projects/90soccer/docs/testflight/INVITE_TEMPLATE.md << 'EOF'
# TestFlight Invitation Templates

## WhatsApp/SMS (Hebrew):
"היי! אתה מוזמן לבדוק את 9Soccer לפני שכולם יראו אותו 🎮
משחק טריוויה כדורגל — 9 שניות לשאלה.
1. הורד TestFlight מה-App Store
2. לחץ על הלינק: [LINK]
אם משהו נראה בעיה — לחץ על הכפתור 🐛 באפליקציה"

## English:
"Hey! You're invited to beta test 9Soccer 🎮
Football trivia — 9 seconds per question.
1. Download TestFlight from App Store
2. Join: [LINK]
See a bug? Tap 🐛 in the app to report it"

## How to get the link:
appstoreconnect.apple.com/apps/6760251165/testflight
→ Click your build → Groups → External Testers
→ Enable Public Link → Copy
EOF
```

### D4. Commit docs update:
```bash
cd /c/Projects/90soccer
git add docs/testflight/
git commit -m "docs: TestFlight build history + invite templates"
git push origin main
```

---

## TASK E — Final Report + MEMORY Update
Agent role: Release coordinator.

### E1. TypeScript check:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -5
npm test 2>&1 | tail -3
```

### E2. Update MEMORY.md.

### E3. Final report:
```
VAMOS 110 COMPLETE
==================

📱 CI Status:
- Build b1ae3a1: [status]
- TestFlight: [distributed / pending]

📸 Screenshots:
- ExplainIt: [can/cannot take screenshots]
- Status: [got 6 screenshots / alternative needed]

🔧 CI fixes:
- Node.js actions: [updated versions]

📋 TestFlight docs:
- Build history: ✅ updated
- Invite templates: ✅ created (HE + EN)

⚠️ Manual still needed:
1. Get TestFlight public link from ASC
   → Share with testers using template in
     docs/testflight/INVITE_TEMPLATE.md

→ Next: VAMOS 111 = first real bug fixes
  from tester feedback
```

---
END VAMOS 110
