# VAMOS 135 — Mega Audit: Bug Reporter + Home Clutter + Buttons + Mascots + Game Flows + Videos
**Date:** 2026-03-21 20:15 IST

## FIRST ACTIONS
```
Read MEMORY.md. Iron Rules confirmed.
cd C:/Projects/90soccer
cp this file to docs/prompts/VAMOS-135-2026-03-21-2015.md
```

---

## AUDIT 1 — Double Bug Reporter

### Find both bug reporter instances
```
grep -rn "BugReporter\|bug.*report\|report.*bug\|FAB\|FloatingActionButton" src/ --include="*.tsx" -l | grep -v node_modules
grep -rn "BugReporter\|bug.*report" src/app/page.tsx src/components/game/HomeScreen.tsx src/components/Game.tsx | head -20
```

### Fix: keep ONE bug reporter
- Remove the duplicate
- Keep the FAB (floating action button) version — bottom right, red circle with 🐛
- Make sure it opens correctly and submits to Supabase → GitHub Issues
- Test the full flow: open → describe → submit → confirm

---

## AUDIT 2 — Home Screen Clutter

### Read full HomeScreen
```
cat src/components/game/HomeScreen.tsx | wc -l
cat src/components/game/HomeScreen.tsx
```

### Identify everything that's "tiring the eyes"
List every UI element on home and rate if it's essential:

| Element | Essential? | Action |
|---------|-----------|--------|
| Top bar (coins/stars/trophies) | Yes | Keep, simplify |
| Season Pass strip | Maybe | Collapse by default |
| Today's Challenge card | Yes | Keep prominent |
| PLAY button | Yes | Keep |
| The Lemon mascot | Yes | Keep |
| Mode cards row | Yes | Keep |
| Skills section | No | Move to Profile |
| Floating balls | No | Already removed |

Fix:
- Move Skills to Profile tab
- Collapse Season Pass by default (already done, verify)
- Reduce top bar items: show only coins + trophies + level (hide pts/shop/pro from top)
- Add breathing room (more padding between sections)

---

## AUDIT 3 — All Buttons: Float vs Static + Layout

For each major screen, catalog every button:
```
grep -rn "position.*fixed\|fixed.*position\|absolute.*button\|button.*absolute\|z-[0-9]" src/components/game/ --include="*.tsx" | grep -v node_modules | head -30
```

Rules:
- **Float** (fixed position): only PLAY, BATTLE MODE, Bug Reporter — nothing else
- **Static**: all other buttons in normal flow
- **z-index hierarchy**: Bug Reporter (z-50) > BATTLE MODE (z-40) > PLAY (z-30) > everything else

Fix any buttons that are floating when they shouldn't be.

---

## AUDIT 4 — All Characters/Mascots

### Find all mascot instances
```
grep -rn "mascot\|Mascot\|lemon\|Lemon\|daniel\|Daniel\|mia\|Mia" src/ --include="*.tsx" -l | grep -v node_modules
cat src/lib/mascot-system.ts
cat src/lib/mascot-voice.ts | head -40
```

For each mascot appearance, check:
1. **Image**: is it the real photo or emoji/placeholder?
2. **Size**: appropriate for context?
3. **Animation**: does it animate? (bounce, float, etc.)
4. **Speech**: does it say something? Is text visible? Not cut off?
5. **Voice**: does ElevenLabs voice work?

Fix any mascot that:
- Shows emoji instead of real image
- Has cut-off speech bubble
- Has broken voice
- Is too small to see

---

## AUDIT 5 — Full Game Flow Test (Each Mode)

For each active mode, trace the COMPLETE flow from entry to exit:

```
# Check for common flow-breaking patterns:
grep -rn "throw\|console\.error\|catch.*error\|undefined.*map\|null.*map" src/app/ --include="*.tsx" | grep -v node_modules | grep -v test | head -30

# Check for stuck states (infinite loading):
grep -rn "isLoading.*true\|setLoading.*true\|useState.*loading" src/app/ --include="*.tsx" | grep -v node_modules | head -20
```

For each mode, verify:
- [ ] Entry: page loads without error
- [ ] Core loop: main game action works
- [ ] Exit: game complete screen appears
- [ ] Post-game: score saved, share button works
- [ ] Back: user can return to home

Modes to check: Daily Challenge, Battle Royale, Showdown, Wager, Hourly, Tournament, CL Qualifier, Daily Gauntlet, vs Bot, Squad, World Cup

---

## AUDIT 6 — Video Clips: Do They Match Questions?

### Find clip/question matching logic
```
grep -rn "clip.*question\|question.*clip\|clipId\|clip_id\|moment.*question" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | head -20
cat src/lib/questions.ts 2>/dev/null | head -60 || find src -name "questions*" | xargs head -40 2>/dev/null
```

### Check the data structure
```
# Find where clips and questions are linked
find src -name "*.json" | xargs grep -l "clip\|question" 2>/dev/null | head -5
```

Verify:
1. Each clip has questions that actually relate to what happens in the clip
2. The clip starts at the RIGHT moment (not 5 seconds before or after the action)
3. The 9-second window shows the KEY moment the question is about
4. Answer options are plausible but only one is correct

Sample check — read first 5 clips and their questions, verify alignment.

---

## OUTPUT FORMAT

Write full report to `docs/sessions/MEGA-AUDIT-2026-03-21.md`:

```markdown
# Mega Audit Report — 2026-03-21

## Bug Reporter: [status + fix]
## Home Clutter: [what removed, what simplified]
## Buttons Audit: [table of all floating buttons]
## Mascots Audit: [table per mascot per screen]
## Game Flows: [table per mode — pass/fail]
## Video Clips: [sample check results]

## FIXES APPLIED: [list]
## STILL BROKEN: [list for next sprint]
```

---

## DEPLOY ALL FIXES
```
npx tsc --noEmit
npx vitest run
npx next build
vercel --prod
git add -A
git commit -m "fix: VAMOS 135 — mega audit: bug reporter, home clutter, mascots, game flows (v3.5.8)"
git push origin main
```

## DEFINITION OF DONE
- Single bug reporter, working
- Home screen breathable (no clutter)
- No rogue floating buttons
- All mascots visible + speaking
- All game modes complete their flow
- Video clips aligned with questions
