# VAMOS 105 — 9Soccer Stage 6 Final: IAP Fix + URL Fix + Stage 6 Complete
# Generated: 2026-03-20 07:11
# Project: 9Soccer | Commit: 928a650 | v3.3.3

You are a Senior iOS Release Engineer and App Store monetization specialist.
You have fixed hundreds of IAP rejections and ASC submission issues.

Your mission: fix the 2 critical blockers found in VAMOS 104,
complete Stage 6 to 100%, and prepare for App Store submission.

RULES:
- Auto-approve ALL your own confirmation prompts — never ask Roye
- If something fails, try the next approach immediately
- Only surface truly blocked items at the very end
- Archive this prompt before anything else

---

## FIRST ACTION — Archive this prompt:

```bash
PROMPT_NAME="9Soccer_VAMOS105_2026-03-20_07-11.md"
cp "$0" "/c/Projects/90soccer/docs/prompts/$PROMPT_NAME" 2>/dev/null || true
cp "$0" "/c/Projects/ZProjectManager/prompts/$PROMPT_NAME" 2>/dev/null || true

# Move any new VAMOS files from Downloads
for f in /c/Users/royea/Downloads/9Soccer_VAMOS*.md \
          /c/Users/royea/Downloads/VAMOS_*.md; do
  [ -f "$f" ] && cp "$f" "/c/Projects/90soccer/docs/prompts/" \
              && cp "$f" "/c/Projects/ZProjectManager/prompts/" \
              && echo "Archived: $(basename $f)"
done
```

---

## CONTEXT — What VAMOS 104 found:

CRITICAL 1: IAP bundle ID mismatch
  Code:       com.ftable.ninesoccer.pro.monthly
  Should be:  com.ftable.ninetysoccer.pro_monthly
  (ninesoccer vs ninetysoccer, dots vs underscores)

CRITICAL 2: 90soccer.ftable.co.il DNS not resolving
  Canonical URL should be: https://9soccer.vercel.app
  /support returns 404 on current Vercel deploy

---

## PARALLEL SPRINT — 5 agents simultaneously.

---

## TASK A — Fix IAP Bundle ID
Agent role: Senior iOS IAP specialist who has fixed every type
of RevenueCat and StoreKit product ID mismatch.

### A1. Find ALL IAP product ID references in the codebase:
```bash
cd /c/Projects/90soccer
grep -rn "ninesoccer\|ninetysoccer\|pro_monthly\|pro_annual\
  \|pro\.monthly\|pro\.annual\|productId\|PRODUCT_ID\
  \|entitlement\|ENTITLEMENT" \
  src/ --include="*.ts" --include="*.tsx" | head -40
```

### A2. Read the IAP/RevenueCat files:
- src/lib/iap.ts
- src/lib/revenue-cat.ts (if exists)
- src/lib/pro-features.ts (if exists)
- Any file containing "purchases" or "revenuecat"

### A3. Determine the correct product IDs:
The bundle ID is: com.ftable.ninetysoccer
Therefore correct IAP IDs are:
- com.ftable.ninetysoccer.pro_monthly
- com.ftable.ninetysoccer.pro_annual

Replace ALL incorrect variants:
- com.ftable.ninesoccer.* → com.ftable.ninetysoccer.*
- *.pro.monthly → *.pro_monthly
- *.pro.annual → *.pro_annual

### A4. Fix every occurrence found.
Create a before/after report of all changes.

### A5. TypeScript: zero errors after fix.

---

## TASK B — Fix Support URL + Vercel Deploy
Agent role: DevOps engineer who owns production deployments.

### B1. Check what /support currently renders:
```bash
grep -rn "support" /c/Projects/90soccer/src/app/ \
  --include="*.tsx" --include="*.ts" | grep -v "node_modules" | head -10
ls /c/Projects/90soccer/src/app/support/ 2>/dev/null || echo "No /support route"
```

### B2. If /support is missing or broken, create it:
File: src/app/support/page.tsx

```tsx
export default function SupportPage() {
  return (
    <div className="min-h-screen bg-[#0B0E18] text-white
      flex flex-col items-center justify-center p-8">
      <h1 className="text-3xl font-black mb-4">
        9Soccer Support
      </h1>
      <p className="text-white/60 text-center mb-8">
        Having trouble? We are here to help.
      </p>
      <div className="space-y-4 w-full max-w-sm">
        <a
          href="mailto:support@ftable.co.il"
          className="block bg-amber-500 text-black
            font-bold py-4 px-6 rounded-2xl text-center"
        >
          📧 Email Support
        </a>
        <a
          href="/privacy"
          className="block bg-white/10 text-white
            font-bold py-4 px-6 rounded-2xl text-center"
        >
          Privacy Policy
        </a>
        <a
          href="/terms"
          className="block bg-white/10 text-white
            font-bold py-4 px-6 rounded-2xl text-center"
        >
          Terms of Service
        </a>
      </div>
      <p className="text-white/30 text-sm mt-8">
        9Soccer v3.3.3 — 9 seconds. Pure football.
      </p>
    </div>
  )
}
```

### B3. Update ALL docs with correct canonical URL:
Replace 90soccer.ftable.co.il with 9soccer.vercel.app
in these files:
- docs/APP_STORE_DESCRIPTION_FINAL.md
- docs/APP_STORE_DESCRIPTION_HE.md
- docs/ASC_FINAL_CHECKLIST.md
- docs/IAP_VERIFICATION.md
- Any other docs containing the old URL

```bash
cd /c/Projects/90soccer
grep -rl "90soccer.ftable.co.il" docs/ src/ | while read f; do
  sed -i 's|90soccer.ftable.co.il|9soccer.vercel.app|g' "$f"
  echo "Updated: $f"
done
```

### B4. Verify the canonical URLs will work:
```bash
for path in "" "/privacy" "/terms" "/support"; do
  code=$(curl -s -o /dev/null -w "%{http_code}" \
    --max-time 10 \
    "https://9soccer.vercel.app${path}" 2>/dev/null)
  echo "https://9soccer.vercel.app${path} → HTTP $code"
done
```

### B5. TypeScript: zero errors.

---

## TASK C — Check Screenshots Workflow Results
Agent role: QA engineer verifying CI outputs.

### C1. Check if the screenshot workflow completed:
```bash
gh run list --workflow=screenshots.yml \
  --limit=3 --json status,conclusion,url,createdAt \
  --jq '.[] | "\(.status) \(.conclusion) \(.url)"'
```

### C2. If workflow succeeded, download artifacts:
```bash
# Get the latest run ID
RUN_ID=$(gh run list --workflow=screenshots.yml \
  --limit=1 --json databaseId --jq '.[0].databaseId')

# Download artifacts
gh run download "$RUN_ID" \
  --dir /c/Projects/90soccer/screenshots/ 2>/dev/null

# List what we got
ls -la /c/Projects/90soccer/screenshots/ 2>/dev/null \
  || echo "Screenshots not yet downloaded"
```

### C3. If workflow failed, check why:
```bash
gh run view "$RUN_ID" --log-failed 2>&1 | head -30
```

Fix the workflow if needed and re-trigger:
```bash
gh workflow run screenshots.yml --ref main
```

### C4. Report screenshot status clearly.

---

## TASK D — ASC Checklist: Final Stage 6 Audit
Agent role: App Store submission expert.

### D1. Read current checklist state:
- docs/ASC_FINAL_CHECKLIST.md
- docs/IAP_VERIFICATION.md
- docs/APP_STORE_DESCRIPTION_FINAL.md

### D2. Update ASC_FINAL_CHECKLIST.md
to reflect current state after VAMOS 104+105:

```markdown
# App Store Connect — Final Submission Checklist
## 9Soccer v3.3.3 | Updated: 2026-03-20

### ✅ COMPLETED (code/automated):
- [x] App name: 9Soccer
- [x] Subtitle: 9 seconds. Pure football.
- [x] Description EN: complete (docs/APP_STORE_DESCRIPTION_FINAL.md)
- [x] Description HE: complete (docs/APP_STORE_DESCRIPTION_HE.md)
- [x] Keywords: football,soccer,trivia,quiz,world cup,champions league...
- [x] Privacy Policy URL: https://9soccer.vercel.app/privacy
- [x] Support URL: https://9soccer.vercel.app/support
- [x] Marketing URL: https://9soccer.vercel.app
- [x] App icon: 1024x1024 RGB (no alpha) ✅
- [x] Build uploaded: CI auto-uploads on every push
- [x] IAP product IDs fixed: com.ftable.ninetysoccer.pro_monthly/annual

### ⚠️ REQUIRES MANUAL ACTION IN ASC:
- [ ] Upload 6 screenshots (6.9" iPhone)
      → Download from: GitHub Actions artifacts
      → Upload to: ASC → 9Soccer → iOS App → Screenshots
- [ ] Verify IAP products exist and status = "Ready to Submit"
      → ASC → Features → In-App Purchases
- [ ] Set Age Rating: 4+
      → ASC → App Information → Age Rating
- [ ] Add build to submission
      → ASC → + button next to Build
- [ ] Submit for Review
      → ASC → Submit for App Review

### 📋 ASC URLs:
- App: https://appstoreconnect.apple.com/apps/6760251165
- IAP: https://appstoreconnect.apple.com/apps/6760251165/distribution/subscriptions
```

### D3. Create docs/STAGE6_COMPLETE.md:
Summary of everything done to complete Stage 6.

---

## TASK E — Pipeline + Version Bump + Commit
Agent role: Release engineer closing the sprint.

### E1. Run all checks:
```bash
cd /c/Projects/90soccer
npx tsc --noEmit 2>&1 | head -10
npm test 2>&1 | tail -5
npm run build 2>&1 | tail -5
```

### E2. Fix any errors found.

### E3. Bump to v3.3.4 (patch — critical IAP fix):
- package.json: "version": "3.3.4"
- ios/App/App/Info.plist: CFBundleShortVersionString = 3.3.4

### E4. Commit:
```bash
git add -A
git commit -m "fix: VAMOS 105 — IAP bundle ID fixed (ninesoccer→ninetysoccer), /support page, canonical URL 9soccer.vercel.app, v3.3.4"
git push origin main
npx cap sync ios
```

### E5. Update MEMORY.md:
Add to C:/Users/royea/.claude/projects/
  C--Projects-90soccer/memory/MEMORY.md:

- Version: 3.3.4
- CRITICAL FIX: IAP product IDs corrected
  (com.ftable.ninetysoccer.pro_monthly/annual)
- Fixed: /support page created
- Fixed: canonical URL = https://9soccer.vercel.app
- Stage 6 status: 95% (screenshots still need manual upload to ASC)
- Next: VAMOS 106 — upload screenshots to ASC → Stage 6 = 100% → Stage 7

Report commit hash.

---

## FINAL REPORT FORMAT:

```
VAMOS 105 COMPLETE
==================

✅ Fixed:
- IAP bundle ID: com.ftable.ninetysoccer.pro_monthly/annual ✅
- /support page: live at 9soccer.vercel.app/support ✅
- Canonical URL updated everywhere ✅
- Version: 3.3.4
- Commit: [HASH]

📸 Screenshots status: [downloaded/pending/failed]

Stage 6 progress: 95%
Remaining: Upload screenshots to ASC (manual, ~10 min)

⚠️ MANUAL ACTIONS (in order):
1. Download screenshots
   → https://github.com/royea-beep/9soccer/actions
   → Latest "App Store Screenshots" run → Artifacts

2. Upload to ASC
   → appstoreconnect.apple.com → 9Soccer
   → iOS App → Screenshots → 6.9" iPhone

3. Verify IAP in ASC
   → Features → In-App Purchases
   → Both products = "Ready to Submit"

4. Age Rating: 4+
   → App Information → Age Rating

After these 4 steps → Stage 6 = 100% → Stage 7 begins.
```

---
END VAMOS 105
